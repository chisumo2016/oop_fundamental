<?php

/*
 * STATIC PROPERTIES  AND STATIC METHODS
 * So far all the properties and methods were linked to a specific instance
 * two different instances could have two different values for same property
 *
 * PHP allows you to have multiple properties and methods linked to the class itself rather than to the object
 * **/